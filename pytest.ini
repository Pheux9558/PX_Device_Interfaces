[pytest]
# fail tests that run longer than 30s to avoid hangs in CI/local runs
timeout = 30
# use thread-based timeout to be compatible with thread-based code
timeout_method = thread
# Restrict discovery to package tests and ignore firmware tool tests that are not runnable here
testpaths = px_device_interfaces
norecursedirs = firmware
